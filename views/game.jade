extends layout

block headers
	title #{config.site_name}

block content
	.container
		h1(style='text-align: center;') #{config.site_name}
		ul.list-group
			+flash_messages
		+game_navbar('home')
		.row
			.col-xs-12
				p Welcome #{user.username}