extends layout

block headers
	title #{config.site_name}

block content
	.container
		.row
			.col-xs-12
				h1 #{config.site_name}
				ul.list-group
					+flash_messages
		.row
			+game_navbar('home')
		.row
			.col-xs-12
				h3 Welcome #{user.username}